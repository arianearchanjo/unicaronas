# Servidor
PORT=3000
NODE_ENV=development

# Banco de Dados PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=unicaronas
DB_USER=postgres
DB_PASSWORD=

# JWT
JWT_SECRET=seu_segredo_jwt_muito_seguro_aqui

# Frontend (CORS)
FRONTEND_URL=http://localhost:5500

# Validação de e-mail institucional (separar por vírgula)
EMAIL_DOMINIOS=@unibrasil.com.br

# Precificação (Novo Método)
GAS_PRICE=7.19
KM_PER_LITER=10
TAXA_PLATAFORMA_PERCENT=10

# Stripe (Pagamentos — opcional para MVP)
STRIPE_SECRET_KEY=sk_test_...
PIX_CHAVE=pagamentos@seudominio.com.br   # ou CPF, telefone, chave aleatória
PIX_NOME=UniCaronas                       # nome que aparece no app do pagador
PIX_CIDADE=Curitiba                       # sua cidade
